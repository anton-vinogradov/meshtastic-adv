# PlatformIO
.pio/
.pioenvs/
.piolibdeps/
*.pyc

# Build artifacts / flashable images
*.bin
*.elf
*.map
*.hex
# ...except the installer images, served same-origin by GitHub Pages
# (the versioned copies feed the installer's version picker)
!docs/firmware.factory.bin
!docs/versions/*/firmware.factory.bin
# ...and the SD unicode font served from the site
!docs/unifont.bin

# Editors / OS
.vscode/
.idea/
.DS_Store
*.swp

# M5Burner combined image (derivative of docs/*.bin)
dist/
